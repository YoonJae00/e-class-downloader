# Project Harness: 한성대 e-Class 영상 자동 다운로더

## 1. 프로젝트 목적 (Context)
한성대학교 e-Class(`learn.hansung.ac.kr`)의 강의 영상을 자동으로 탐색하고 MP4로 변환하여 저장하는 CLI 도구입니다. 복잡한 SSO 로그인 및 JW Player 구조를 자동화하여 학생들의 학습 편의를 돕는 것이 목적입니다.

## 2. 구현된 기능 (Current State)
- **자동 로그인 (Playwright)**: Chromium Headless 브라우저를 통해 아이디/비번 기반 자동 로그인 및 `MoodleSession` 쿠키 자동 추출.
- **세션 유지**: 추출된 쿠키를 `config.json`에 저장하여 유효 기간 동안 재사용.
- **강의 크롤링**: 수강 과목 목록 및 과목별 주차/영상 리소스 자동 추출.
- **스마트 다운로드**: `yt-dlp` + `static-ffmpeg` 연동으로 별도 프로그램 설치 없는 MP4 변환 저장.
- **사용자 UI**: 과목 선택 후 개별 영상 선택(`1,2,5`) 또는 전체 선택(`all`) 기능 제공.
- **Mac 실행 편의**: 더블 클릭으로 실행 가능한 `download_lecture.command` 제공.

## 3. 핵심 기술 패턴 (Key Patterns)
- **Auth**: `playwright`를 이용해 실제 브라우저 환경에서 로그인을 수행한 뒤 `requests.Session` 및 `yt-dlp`의 `cookiejar`에 쿠키를 주입하는 패턴 사용.
- **Parsing**: JW Player의 JavaScript 소스 내 `file: '...'` 패턴을 정규식으로 추출하여 m3u8 주소 획득.
- **File System**: `./downloads/{강의명}/{강의명}_{주차}_{순서}_{제목}.mp4` 구조로 자동 분류.

## 4. 향후 개선 및 수정 사항 (Roadmap & Issues)
- [ ] **세션 만료 처리**: 저장된 쿠키가 만료되어 403 에러 발생 시, 자동으로 브라우저를 다시 띄워 갱신하는 로직 보강 필요.
- [ ] **DRM 대응**: 현재는 표준 HLS(m3u8) 기반이나, 향후 DRM 도입 시 `yt-dlp` 옵션 조정 또는 대응 불가능할 수 있음.
- [ ] **병렬 다운로드**: 현재는 순차 다운로드 방식임. `threading`이나 `asyncio`를 활용해 여러 영상을 동시에 받는 기능 고려.
- [ ] **코드 모듈화**: `main.py`에 집중된 로직을 `auth.py`, `crawler.py`, `downloader.py`로 분리하여 가독성 향상.
- [ ] **예외 페이지 대응**: 퀴즈나 과제만 있는 주차에서 영상이 없음을 더 명확히 안내하는 로직.

## 5. 실행 환경 (Environment)
- **Language**: Python 3.12+
- **Main Deps**: `playwright`, `yt-dlp`, `static-ffmpeg`, `beautifulsoup4`, `requests`
- **Initial Setup**: `pip install -r requirements.txt && playwright install chromium`
