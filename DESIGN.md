# DESIGN: 한성대학교 e-Class 영상 자동 다운로더

## 1. 개요 (Overview)
본 프로젝트는 한성대학교 e-Class(`learn.hansung.ac.kr`) 시스템에 로그인하여 수강 중인 과목의 강의 영상을 자동으로 탐색하고, 이를 MP4 형식으로 변환하여 저장하는 CLI 도구입니다.

## 2. 요구사항 (Requirements)

### 기능적 요구사항
- **로그인 관리**: `config.json`에 학번과 비밀번호를 저장하고 자동으로 로그인 세션을 유지합니다.
- **강의 탐색**: 메인 페이지에서 수강 중인 과목 목록을 가져와 사용자에게 선택지를 제공합니다.
- **영상 추출**: 선택한 과목의 주차별 섹션을 분석하여 VOD 리소스의 `index.m3u8` 주소를 추출합니다.
- **자동 다운로드**: 추출된 m3u8 주소를 `yt-dlp` 엔진을 통해 MP4로 변환하여 다운로드합니다.
- **파일명 규칙**: `{과목명}_{주차}_{순서}.mp4` 형식으로 저장하며, 과목별로 하위 폴더를 생성합니다.

### 비기능적 요구사항
- **독립성**: 사용자가 별도로 `ffmpeg`를 설치하지 않아도 작동하도록 `static-ffmpeg`를 사용합니다.
- **안정성**: 네트워크 오류 시 재시도 로직을 포함하며, 파일명에 사용할 수 없는 특수문자를 자동으로 치환합니다.

## 3. 기술 스택 (Tech Stack)
- **Language**: Python 3.x
- **Libraries**:
    - `requests`: HTTP 세션 및 페이지 요청 관리
    - `BeautifulSoup4`: HTML 파싱 및 데이터 추출
    - `yt-dlp`: 영상 스트림 다운로드 및 변환 핵심 엔진
    - `static-ffmpeg`: ffmpeg 바이너리 내장 제공

## 4. 데이터 흐름 (Data Flow)
1.  `config.json` 로드 또는 사용자 입력 생성
2.  LMS 로그인 API 호출 및 세션 쿠키 획득
3.  과목 목록 크롤링 및 사용자 선택 대기
4.  과목 상세 페이지 접속 -> 주차별 VOD 링크 수집
5.  VOD 뷰어 페이지 접속 -> m3u8 URL 추출
6.  `yt-dlp`에 쿠키 및 URL 전달 -> MP4 저장

## 5. 예외 처리 (Error Handling)
- 로그인 실패 시 에러 메시지 출력 및 재입력 유도
- 이미 존재하는 파일은 다운로드 건너뛰기 (Skip)
- 파일명 금칙어 치환 ( \ / : * ? " < > | )

## 6. 결정 기록 (Decision Log)
| 결정 항목 | 선택된 방안 | 이유 |
| :--- | :--- | :--- |
| 인터페이스 | CLI | 구현 속도가 빠르고 개발자/학생 사용에 적합함 |
| 인증 정보 관리 | `config.json` 저장 | 매번 입력하는 번거로움 해결 |
| 다운로드 엔진 | `yt-dlp` | m3u8 처리 능력이 가장 뛰어나고 안정적임 |
| ffmpeg 처리 | `static-ffmpeg` 패키지 | 별도 설치 없이도 변환 기능을 제공하기 위함 |
